# Ferramentas

Stripe expõe 1 ferramenta (todas somente leitura).

### 1. `search_tools`
**Input**: `action` (opcional), `query` (opcional), `min_score` (opcional), `force_reindex` (opcional), `queries` (opcional), `tool_id` (opcional), `arguments` (opcional)

Single entrypoint for MCP catalog.

## Prompts de exemplo

```
Qual o saldo disponível na minha conta Stripe?
Liste os últimos 10 pagamentos (payment intents)
Crie um cliente com email cliente@exemplo.com
```
